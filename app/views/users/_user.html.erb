<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && user && !user.admin? &&  %>
    | <%= link_to "Delete", user_path(user), method: :delete, data: {confirm: "You sure?"} %>
  <% end %>
</li>
